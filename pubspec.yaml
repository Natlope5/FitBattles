name: fitbattles
description: "Fit Battles is a dynamic mobile application designed to encourage users to engage in fitness challenges and track their progress. The app allows users to create and join fitness battles with friends, compete in various activities, and earn rewards for their achievements. Leveraging Firebase services, Fit Battles provides real-time updates, user authentication, and cloud storage for user-generated content.

Key features include:

- **User Authentication**: Secure sign-up and login through Firebase Authentication, ensuring a personalized experience.
- **Challenge Creation**: Users can create and customize fitness challenges to motivate themselves and their friends.
- **Progress Tracking**: Track workouts, monitor progress, and visualize achievements with user-friendly dashboards.
- **Real-Time Updates**: Stay connected with friends and receive notifications about their fitness activities using Firebase Cloud Messaging.
- **Media Sharing**: Upload and share workout photos and videos via Firebase Storage.

Fit Battles aims to foster a supportive fitness community while making the journey toward a healthier lifestyle engaging and fun."
publish_to: '' # Leave empty or remove for a private package

version: 1.0.0+1

environment:
  sdk: ^3.5.3

dependencies:
  flutter:
    sdk: flutter

  flutter_launcher_icons: ^0.10.0
  dio: ^5.2.1
  firebase_core: ^3.5.0
  firebase_auth: ^5.3.0
  cloud_firestore: ^5.2.0
  firebase_storage: ^12.0.0
  firebase_messaging: ^15.1.2
  http: ^1.2.2
  provider: ^6.0.0
  image_picker: ^1.1.2
  health: ^11.0.0
  dart_format: ^1.1.20
  firebase_analytics: ^11.3.2
  firebase_database: any
  firebase_ui_database: ^1.4.4
  logger: ^2.4.0
  shared_preferences: ^2.3.2
  geolocator: ^13.0.1
  cupertino_icons: ^1.0.8
  getwidget: ^4.0.0
  firebase_app_check: ^0.3.1+3
  googleapis_auth: ^1.6.0
  analyzer: ^6.8.0
  material_color_utilities: ^0.11.1
  meta: ^1.15.0
  mime: ^1.0.6
  stack_trace: ^1.11.1
  vm_service: ^14.2.5
  intl: ^0.19.0
  back_button_interceptor: ^7.0.3
  timezone: ^0.9.4
  camera: ^0.11.0
  path_provider: ^2.0.11
  permission_handler: ^11.3.1
  in_app_update: ^4.2.3
  video_player: ^2.4.6
  fl_chart: ^0.69.0
  easy_localization: ^3.0.0
  fluttertoast: ^8.0.9
  flutter_localizations:
    sdk: flutter # Required for localization support

  flutter_staggered_animations: ^1.0.0
  file_picker: ^8.1.2
  theme_tailor: ^3.0.1
  packme: ^2.0.2
  android_intent_plus: ^5.1.0
  flutter_local_notifications: ^17.2.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter_icons:
  android: true
  ios: true
  image_path: "assets/icons/your_image.png"

flutter:
  uses-material-design: true
  generate: true
  assets:
    - assets/images/logo.png
    - assets/images/logo2.png
    - assets/images/placeholder_avatar.png
    - assets/images/Bob.png
    - assets/images/Charlie.png
    - assets/images/Hannah.png
    - assets/images/Ian.png
    - assets/images/Fiona.png
    - assets/images/George.png
    - assets/images/Ethan.png
    - assets/images/Diana.png
    - assets/images/Alice.png
    - lib/l10n/int1_de.json
    - lib/l10n/int1_en.json
    - lib/l10n/int1_es.json
    - lib/l10n/int1_fr.json
    - lib/l10n/int1_zh.json